# Dependencies
node_modules/

# Build output (release artifacts are attached to GitHub Releases, not committed)
main.js
main.js.map
*.js.map

# Plugin runtime data
data.json

# Test / coverage
coverage/

# Local dev deploy target (machine-specific vault path), incl. parked
# variants like .deploy-target.off
.deploy-target*

# Local agent workspace (session plans can contain private infra details)
.claude/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.log

.proj
